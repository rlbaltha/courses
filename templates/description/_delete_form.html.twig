<form style="margin-top: 1em !important;" method="post" action="{{ path('description_delete', {'id': description.id, 'courseid': courseid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ description.id) }}">
    <button class="cmp-button">Delete</button>
</form>
